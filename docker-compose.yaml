version: '3.8'

services:
  wayvault:
    build: .
    container_name: wayvault-backend
    restart: always
    ports:
      - "3033:8000"
    volumes:
      - ./wayvault.db:/app/wayvault.db
    environment:
      - PYTHONUNBUFFERED=1
